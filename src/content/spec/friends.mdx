---
title: "友情链接"
description: "与优秀的朋友们一起成长"
---

{/* MDX 文件可以根据自己的喜好完全重写，默认提供的布局仅作为参考模板，也可以使用常规的markdown语法。 */}

{/* ========== 站点信息 ========= */}

export const site = {
  name: "CXL 避难所",
  desc: "一位Arch巫师的赛博据点。服务器？不存在的。",
  url: "https://cxl--package.top",
  avatar: "https://github.com/PAleimiao.png",
  email: "",
};

{/* ========= 注意事项 ========== */}

export const notes = [
  { title: "互换原则", content: "请先将本站添加到您的友链页面，确认后会添加您的友链" },
  { title: "链接维护", content: "友链网站长期无法访问或内容违规，将会被移除" },
  { title: "内容要求", content: "内容积极向上，不含有任何违法违规内容" },
  { title: "站点要求", content: "支持 HTTPS，能够正常访问且有持续更新" },
];

{/* ============================= */}

<div class="not-prose my-4">
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
    {/* 左栏：本站信息 */}
    <div class="rounded-2xl border border-(--line-divider) overflow-hidden flex flex-col">
      <div class="p-5 sm:p-6 flex flex-col flex-1">
        {/* 头部：头像和名称 */}
        <div class="flex items-center gap-3 mb-4">
          <img 
            src={site.avatar} 
            alt={site.name}
            class="w-12 h-12 rounded-full object-cover border border-(--line-divider)"
          />
          <div>
            <h3 class="text-lg font-bold text-(--primary)">{site.name}</h3>
            <p class="text-sm text-(--secondary)">{site.desc}</p>
          </div>
        </div>

        {/* 信息列表 */}
        <div class="space-y-2 flex-1">
          <div class="flex items-center gap-2 text-sm">
            <span class="text-(--secondary) w-16 shrink-0">网站地址</span>
            <a 
              href={site.url} 
              target="_blank" 
              rel="noopener noreferrer"
              class="text-(--primary) hover:underline truncate"
            >
              {site.url}
            </a>
          </div>
          {site.email && (
            <div class="flex items-center gap-2 text-sm">
              <span class="text-(--secondary) w-16 shrink-0">电子邮箱</span>
              <span class="text-(--primary)">{site.email}</span>
            </div>
          )}
        </div>
      </div>
    </div>

    {/* 右栏：申请友链 */}
    <div class="rounded-2xl border border-(--line-divider) overflow-hidden flex flex-col">
      <div class="p-5 sm:p-6 flex flex-col flex-1">
        <h3 class="text-lg font-bold text-(--primary) mb-4">申请友链</h3>
        <div class="space-y-3 flex-1">
          {notes.map((note, index) => (
            <div key={index} class="flex gap-3">
              <span class="text-sm font-medium text-(--primary) w-20 shrink-0">{note.title}</span>
              <span class="text-sm text-(--secondary)">{note.content}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</div>

{/* 友链列表 */}
<div class="not-prose my-8">
  <h2 class="text-xl font-bold text-(--primary) mb-4">朋友们</h2>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
    {/* 示例友链，请根据实际情况修改 */}
    <a 
      href="https://github.com/CuteLeaf/Firefly" 
      target="_blank" 
      rel="noopener noreferrer"
      class="rounded-2xl border border-(--line-divider) p-4 hover:border-(--primary) transition-colors group"
    >
      <div class="flex items-center gap-3">
        <img 
          src="https://github.com/CuteLeaf.png" 
          alt="Firefly"
          class="w-10 h-10 rounded-full object-cover border border-(--line-divider)"
        />
        <div>
          <h4 class="font-bold text-(--primary) group-hover:text-(--primary)">Firefly</h4>
          <p class="text-xs text-(--secondary) truncate">本站使用的博客主题</p>
        </div>
      </div>
    </a>
  </div>
</div>
